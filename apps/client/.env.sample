# Error reporting (Sentry), leave blank to disable, the SDK then stays inert
VITE_SENTRY_DSN=
# shown as the environment on each issue, set to uat on the UAT deploy
VITE_SENTRY_ENVIRONMENT=development
